document.getElementById('login-form').addEventListener('submit', function(event) {
    event.preventDefault();
    // Perform login validation here (for simplicity, we'll skip it)
    window.location.href = 'select-robot.html';
});
